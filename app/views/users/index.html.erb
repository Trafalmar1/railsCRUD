<h1>Hello Rails</h1>

There are <%= User.public_count%> users

<ul>
    <% @users.each do |user|%>
        <% unless user.archived?%>
            <li>
                <%= link_to user.username, user%>
            </li>
        <%end%>
    <%end%>
</ul>

<%= link_to "New User",new_user_path%>